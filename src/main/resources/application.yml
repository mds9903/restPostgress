app:
  name: inventory-app-default

# for availability api stock level
demandTypes: HARD_PROMISED
supplyTypes: ONHAND
locations:
  exclude: '18100'

# spring
spring:
  #  profiles:
  #    active: dev
  # mongodb
  data:
    mongodb:
      uri: "mongodb+srv://learner:4748@cluster34938.uh7rhpp.mongodb.net/?retryWrites=true&w=majority"
      database: "db_inventory_app"
  security:
    user:
      name: "mds9903"
      password: "4748"

# logging
logging:
  file:
    name: app.log
  level:
    com:
      mdsujan:
        restPostgres:
          controller: trace


# for app's local running
server:
  port: '8088'
  error:
    include-message: always